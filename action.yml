name: 'Local Cache on Self-Hosted'
description: 'Cache dependencies and build outputs to improve workflow execution time on self hosted machine'

inputs:
  path:
    description: 'File or folder to be cached'
    required: true
  action:
    description: 'Action will be run if cache not found'
    required: true
  cache-key:
    description: 'Explicit key for versioning the cache'
    required: true
  cache-dir:
    description: 'Path dir will cache'
    required: true
  
outputs:
  cache-hit:
    description: 'A boolean value to indicate if cache was found and restored'

branding:
  icon: 'play'
  color: 'green'

runs:
  using: 'composite'
  steps:
    - name: Store Inputs Value
      shell: bash
      id: inputs_file
      run: |
        echo "TEMPORARY_FILE=$(pwd)/inputs.txt" >> $GITHUB_ENV
        echo "${{ inputs.path }}" > $TEMPORARY_FILE
        echo "${{ inputs.action }}" >> $TEMPORARY_FILE
        echo "${{ inputs.cache-key }}" >> $TEMPORARY_FILE
        echo "${{ inputs.cache-dir }}" >> $TEMPORARY_FILE
        echo "${{ inputs.cache-dir }}/${{ inputs.cache-key }}" >> $TEMPORARY_FILE

    - name: Check Inputs Value
      shell: bash
      run: source ${{ github.action_path }}/scripts/check_inputs.sh

    - name: Cache Restore
      shell: bash
      id: check_cache
      run: source ${{ github.action_path }}/entrypoint.sh

    - name: Clean Inputs File
      shell: bash
      run: rm -rf ${{ env.TEMPORARY_FILE }}
